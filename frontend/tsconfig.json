// frontend/tsconfig.json
{
  // --- ДОБАВЛЕНЫ ОПЦИИ ДЛЯ SHADCN ---
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  // ----------------------------------
  "files": [], // Оставляем пустым, т.к. файлы указаны в references
  "references": [
    // Ссылки на специфичные конфигурации
    { "path": "./tsconfig.app.json" }, 
    { "path": "./tsconfig.node.json" } 
  ]
}